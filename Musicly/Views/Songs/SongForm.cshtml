@model Musicly.ViewModels.SongFormViewModel   
@{
    ViewBag.Title = Model.Title;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Title</h2>

@using (Html.BeginForm("Save", "Songs"))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Song.Name)
        @Html.TextBoxFor(m => m.Song.Name, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Song.ReleaseDate)
        @Html.TextBoxFor(m => m.Song.ReleaseDate, "{0:d MMM yyyy}", new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Song.NumberInStock)
        @Html.TextBoxFor(m => m.Song.NumberInStock, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Song.GenreId)
        @Html.DropDownListFor(m => m.Song.GenreId, new SelectList(Model.Genres, "Id", "Name"), "Select Genre", new { @class = "form-control" })
    </div>

    @Html.HiddenFor(m => m.Song.Id)

    <button type="submit" class="btn btn-primary">Save</button>
}
